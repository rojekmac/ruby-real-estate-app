<!-- Hero Section -->
<div class="hero-section">
  <div class="container hero-container" style="display: flex; align-items: center; justify-content: center; gap: 4rem;">
    <div class="hero-content">
      <h1 class="hero-title"><%= t('hero_title') %></h1>
      <p class="hero-subtitle"><%= t('hero_subtitle') %></p>
      <div class="hero-buttons">
        <%= link_to t('hero_cta_primary'), pages_listings_path, class: "btn btn-primary" %>
        <a class="btn btn-secondary" href="<%= pages_listings_path %>"><%= t('hero_cta_secondary') %></a>
      </div>
    </div>
    <div class="hero-image">
      <%= image_tag 'thai_logo.jpeg', alt: 'Dream Home', class: 'hero-img' %>
    </div>
  </div>
</div>

<!-- Features Section -->
<div class="features-section">
  <div class="container">
    <h2 class="section-title"><%= t('features_title') %></h2>
    <div class="features-grid">
      <div class="feature-card">
        <div class="feature-icon">
          <i class="fas fa-search"></i>
        </div>
        <h3><%= t('feature_expertise_title') %></h3>
        <p><%= t('feature_expertise_desc') %></p>
      </div>
      <div class="feature-card">
        <div class="feature-icon">
          <i class="fas fa-comments"></i>
        </div>
        <h3><%= t('feature_service_title') %></h3>
        <p><%= t('feature_service_desc') %></p>
      </div>
      <div class="feature-card">
        <div class="feature-icon">
          <i class="fas fa-handshake"></i>
        </div>
        <h3><%= t('feature_quality_title') %></h3>
        <p><%= t('feature_quality_desc') %></p>
      </div>
    </div>
  </div>
</div>

<!-- Quick Actions Section -->
<div class="quick-actions-section">
  <div class="container">
    <h2 class="section-title"><%= t('actions_title') %></h2>
    <div class="actions-grid">
      <%= link_to pages_listings_path, class: "action-card" do %>
        <div class="action-icon">
          <i class="fas fa-list"></i>
        </div>
        <h3><%= t('action_search_title') %></h3>
        <p><%= t('action_search_desc') %></p>
      <% end %>
      
      <%= link_to properties_search_path, class: "action-card" do %>
        <div class="action-icon">
          <i class="fas fa-search"></i>
        </div>
        <h3><%= t('action_search_title') %></h3>
        <p><%= t('action_search_desc') %></p>
      <% end %>
      
      <%= link_to properties_developments_path, class: "action-card" do %>
        <div class="action-icon">
          <i class="fas fa-building"></i>
        </div>
        <h3><%= t('action_valuation_title') %></h3>
        <p><%= t('action_valuation_desc') %></p>
      <% end %>
      
      <%= link_to services_valuation_path, class: "action-card" do %>
        <div class="action-icon">
          <i class="fas fa-calculator"></i>
        </div>
        <h3><%= t('action_contact_title') %></h3>
        <p><%= t('action_contact_desc') %></p>
      <% end %>
    </div>
  </div>
</div>

<!-- About Section -->
<div id="about-section" class="about-section">
  <h2 class="section-title"><%= t('about_title') %></h2>
  <p class="about-text">
    <%= t('about_content') %>
  </p>
</div>

<!-- Contact Section -->
<div id="contact-section" class="contact-section">
  <h2 class="section-title"><%= t('contact_title') %></h2>
  <p class="contact-text">
    <%= t('contact_content') %>
  </p>
  <div class="contact-details">
    <strong><%= t('contact_name') %>:</strong> Tobias Schwitalla<br>
    <strong><%= t('contact_email') %>:</strong> tobias.schwitalla@example.com<br>
    <strong><%= t('contact_phone') %>:</strong> +66 81 234 5678<br>
    <strong><%= t('contact_location') %>:</strong> Bangkok, Thailand
  </div>
</div>

<script>
  document.addEventListener('turbo:load', function() {
    if (window.location.hash) {
      const target = document.getElementById(window.location.hash.slice(1));
      if (target) {
        target.scrollIntoView({ behavior: 'smooth' });
      }
    }
    const anchorLinks = document.querySelectorAll('a[href^="#"], a[href^="/"][href*="#"]');
    anchorLinks.forEach(function(link) {
      link.addEventListener('click', function(e) {
        const href = this.getAttribute('href');
        const hash = href.includes('#') ? href.split('#')[1] : null;
        if (hash) {
          const target = document.getElementById(hash);
          if (target) {
            e.preventDefault();
            history.pushState(null, '', '#' + hash);
            target.scrollIntoView({ behavior: 'smooth' });
          }
        }
      });
    });
  });
</script>